# Source archive

The complete tracked source is split into four parts because of GitHub upload limits. Download all four files in this directory, then run:

```bash
cat cookie-run-source.part-* > cookie-run-source.tar.gz
tar -xzf cookie-run-source.tar.gz
```

The archive excludes local caches, dependencies, old screenshots, internal agent metadata, and personal documents.
