/**
 * Generated by orval v8.21.0 🍺
 * Do not edit manually.
 * Api
 * API specification
 * OpenAPI spec version: 0.1.0
 */
import type { WorkspaceInputCurrency } from './workspaceInputCurrency';
import type { WorkspaceInputInstanceType } from './workspaceInputInstanceType';

export interface WorkspaceInput {
  /** @minLength 1 */
  gpuModel: string;
  /**
     * @minimum 1
     * @maximum 720
     */
  durationHours: number;
  /** Native COOK on Cookie Chain is the active checkout path. */
  currency?: WorkspaceInputCurrency;
  /** Compute workload template id from GET /templates (omit for bare GPU). */
  templateId?: string;
  /**
     * Public HTTPS GitHub repository to bootstrap into /workspace/repository.
     * @maxLength 2048
     */
  repositoryUrl?: string;
  /**
     * Branch, tag, or commit to check out. Defaults to the repository default branch.
     * @maxLength 256
     */
  repositoryRevision?: string;
  /**
     * Opaque short-lived GitHub credential reference.
     * @minLength 32
     */
  githubCredentialRef?: string;
  /**
     * Public OCI image override. Registry credentials are not accepted or stored.
     * @maxLength 255
     */
  containerImage?: string;
  /**
     * Opaque short-lived registry credential reference.
     * @minLength 32
     */
  registryCredentialRef?: string;
  /** Capacity type. spot is interruptible and cheapest; reserved is a committed discount. */
  instanceType?: WorkspaceInputInstanceType;
  /**
     * Extra persistent disk (GB) added on top of the template's bundled default.
     * @minimum 0
     * @maximum 500
     */
  extraDiskGb?: number;
  email?: string;
  payerWallet?: string;
  /** SSH public key to inject into the instance (e.g. "ssh-rsa AAAA…") */
  sshKey?: string;
  /**
     * When set, launches an Agent Run: a coding agent works this task inside the instance against the public repository given by repositoryUrl/repositoryRevision. Requires a public repositoryUrl. The instance's SSH access is generated and held by the server for the run; a pasted sshKey is optional and only adds your own terminal access alongside the agent's.
     * @minLength 1
     * @maxLength 4000
     */
  agentTask?: string;
}
